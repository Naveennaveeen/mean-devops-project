name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Backend Image
        working-directory: backend
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/mean-backend:latest .

      - name: Push Backend Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/mean-backend:latest

      - name: Build Frontend Image
        working-directory: frontend
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/mean-frontend:latest .

      - name: Push Frontend Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/mean-frontend:latest
